import { environment } from 'src/environments/environment';

export const CHANGE_PASSWORD = `${environment.api}/auth/password`;
export const CHANGE_EMAIL = `${environment.api}/auth/email`;
export const BLOCK_ACCOUNT = `${environment.api}/auth/user`;
